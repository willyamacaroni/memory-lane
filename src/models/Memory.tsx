export interface Memory {
  id: number;
  lane_id: number;
  name: string;
  description: string;
  timestamp: string;  // Adjust based on the actual date format you intend to use
  image: string;
}

